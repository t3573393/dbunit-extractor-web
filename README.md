# dbunit-extractor-web
dbunit extractor web application, for the generation of the dbunit xml file.

## Usage  

First config the db connections, then use the sql or the table name to preview or export the xml file.  